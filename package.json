{
  "name": "planning-poker-e2e",
  "version": "1.0.0",
  "description": "E2E tests for Planning Poker",
  "private": true,
  "scripts": {
    "e2e:test": "cd test/e2e && npx playwright test",
    "e2e:ui": "cd test/e2e && npx playwright test --ui",
    "e2e:headed": "cd test/e2e && npx playwright test --headed",
    "e2e:debug": "cd test/e2e && npx playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
